#cat usersfile.yml
names:
  - rahul
  - amar
  - akbar
  - antony
  - russy

#cat creating_users_repeat.yml
- hosts: node2
  become: yes
  vars_files:
    - usersfile.yml
  tasks:
    - name: "making users"
      user:
         name: '{{item}}'
         state: present
           #      loop: '{{names}}'      #used for newer version
      with_items: '{{names}}'
#if we are giving like this:
with_items/loop:
      - '{{names}}'      #toh ye error dega aur nai chalega.



OR

- name: i am creating a lookup file wala ansible file
  hosts: rhel
  become: yes
  tasks:
    - name: create user by looking the file
      user:
        name: "{{item}}"
        state: present
        remove: yes
      with_lines: cat names.txt
      #with_lines har line read karega ek-2 krke.

# cat names.txt
rohan
ajya
ridhima


